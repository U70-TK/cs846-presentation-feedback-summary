# Problem D Database

This folder is intended to hold the SQLite database used by the demo project.

- Suggested file: `northwind_signal.sqlite`
- Keep the database small so it is easy to inspect during the experiment.
- Use `seed.sh` to create and seed the database.
